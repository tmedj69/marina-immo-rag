# === Collecte YouTube ===
google-api-python-client==2.120.0   # YouTube Data API v3
youtube-transcript-api==0.6.2       # Transcriptions (gratuit, hors quota API)

# === LLM & Embeddings ===
openai==1.30.0                      # GPT-4o-mini pour extraction + embeddings

# === Stockage ===
chromadb                    # Base vectorielle locale (comme ton projet hadith)

# === Utilitaires ===
python-dotenv==1.0.0                # Lecture du fichier .env
pyyaml==6.0.1                       # Lecture du fichier sources.yaml